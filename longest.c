#include<stdio.h>
#include<string.h>

int main() {
  int n,i;
  char v[50],s[50],e[50],b[50];
  gets(v);
  gets(s);
  gets(b);
  n=strlen(a);
  for(i=0;i<n;i++)
  {
      if(v[i]==s[i])
      {
        b[i]=v[i];
      }else
      {
          break;
      }
  }for(i=0;i<n;i++)
  {
      if(s[i]==b[i])
      {
          printf("%c",c[i]);
      }else
      {
          break;
      }
  }
    
}

